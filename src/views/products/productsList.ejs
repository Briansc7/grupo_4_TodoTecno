<!DOCTYPE html>
<html lang="en">

    <%- include("../partials/head",{title: head.title, stylesheet: head.stylesheet}) %>

<body>
    <%- include("../partials/header") %>

    <main>
        <div class="container products-wrapper">
		
            <div class="row">
                <div class="col-12 products-title-container">
                    <h2 class="products-title">Todos los productos:</h2>
                </div>
    
                <% for(i=0; i<products.length; i++) { %>
    
                    <div class="col-12 col-sm-6 col-lg-3 product-container">
                        <section class="product-box">
                            <a href="/products/productDetail/<%= products[i].id %>">
                                <figure class="product-box_image">
                                    <img src="/images/products/<%= products[i].images[0] %>" alt="imagen de producto">
                                </figure>
                                <article class="product-box_data">
                                    <h2>$<%= products[i].price %></h2>
                                    <span><%= products[i].discount %>% OFF</span>
                                    <p><%= products[i].category %> <%= products[i].brand %> <%= products[i].model %></p>
                                </article>
                            </a>
                        </section>
                    </div>
    
                <% } %>           
                
            </div>
        </div>
    </main>

    <%- include("../partials/footer") %>
</body>
</html> 